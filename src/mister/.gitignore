# Quartus build artifacts
db/
incremental_db/
output_files/
greybox_tmp/
simulation/
*.qws
*.rpt
*.summary
*.pin
*.done
*.smsg
*.sof
*.rbf
*.jdi
*.cdf
PLLJ_PLLSPE_INFO.txt
c5_pin_model_dump.txt

# generated by sys/build_id.tcl at every compile
build_id.v

# Assembled ROM images are NEVER committed (see the repo-root .gitignore and
# support/package.sh). The .mra in releases/ is XML that references MAME chips
# by CRC32 - it contains no ROM data and is safe to commit.
*.rom
*.zip
*_assignment_defaults.qdf
